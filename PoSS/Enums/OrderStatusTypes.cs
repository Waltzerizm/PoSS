namespace PoSS.Enums
{
    public enum OrderStatusTypes
    {
        NEW = 0,
        ACCEPTED = 1,
        IN_PROGRESS = 2,
        AWAITING_SHIPMENT = 3,
        SHIPPED = 4,
        AWAITING_PICKUP = 5,
        COMPLETED = 6,
        REJECTED = 7,
        CANCELLED = 8,
        REFUNDED = 9
    }
}
